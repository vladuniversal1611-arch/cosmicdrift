# Node / build
node_modules/
src/generated/

# The Android project is committed so the build is just `npm install` +
# `npm run cap:sync` (its own android/.gitignore excludes build outputs).
/ios/

# Build-generated web root (regenerated by `npm run build`)
/www/
