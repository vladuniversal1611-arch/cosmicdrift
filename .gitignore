# Dependencies
node_modules/

# The native Android project (android/) IS committed so the repo can be
# downloaded and built into an AAB in Android Studio with no extra setup.
# Build outputs inside it are excluded by android/.gitignore.
!/android-resources/
*.apk
*.aab
*.keystore
*.jks

# OS / editor
.DS_Store
Thumbs.db
.idea/
.vscode/
*.log
