{
  "name": "skydoku",
  "version": "1.0.0",
  "description": "Skydoku — a bright block puzzle with cute character blocks.",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "cap:sync": "npm run build && cap sync",
    "cap:open": "cap open android",
    "cap:run": "npm run build && cap sync && cap open android",
    "icons": "capacitor-assets generate --android"
  },
  "dependencies": {
    "@capacitor-community/admob": "^6.1.0",
    "@capacitor/android": "^6.1.2",
    "@capacitor/core": "^6.1.2",
    "@capacitor/local-notifications": "^6.1.0",
    "@capacitor/status-bar": "^6.0.1"
  },
  "devDependencies": {
    "@capacitor/assets": "^3.0.5",
    "@capacitor/cli": "^6.1.2",
    "esbuild": "^0.23.1"
  }
}
