{
  "name": "dragon-merge-blast",
  "version": "1.0.0",
  "description": "Dragon Merge Blast — сучасна мобільна гра 3-в-ряд з драконами для Android.",
  "license": "MIT",
  "scripts": {
    "serve": "node server.js",
    "cap:add": "cap add android",
    "cap:sync": "cap sync android",
    "cap:open": "cap open android",
    "android:build": "cap sync android && cd android && ./gradlew assembleDebug",
    "android:release": "cap sync android && cd android && ./gradlew bundleRelease"
  },
  "dependencies": {
    "@capacitor/android": "^6.1.2",
    "@capacitor/core": "^6.1.2",
    "@capacitor/splash-screen": "^6.0.2",
    "@capacitor/status-bar": "^6.0.1"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.1.2"
  }
}
